<% unless @feed_items.empty? %>
	<%= will_paginate @feed_items, :param_name => 'feed_page' %>	
	<table class="sidebar round" summary="User feeds">
		<%= render :partial => 'shared/feed_item', :collection => @feed_items%>
	</table>
<% end %>
