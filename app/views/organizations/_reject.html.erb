<% @club = Organization.find(params[:id]) %>
<% session[:current_club] = @club.id %>
<%= form_for @club.reverse_org_relationships.find_by_member_id(user), 
	html: { method: :delete },
	remote: true do |f| %>
	<%= f.submit "Reject", class: "js_actions" %>
<% end %>
