<%= form_for current_user.org_relationships.build(club_id: <% @club = current_club.id %>.id), remote: true do |f| %>
	<div><%= f.hidden_field :club_id %></div>
	<%= f.submit "Join", class: "actions" %>
<% end %>                                                                        
